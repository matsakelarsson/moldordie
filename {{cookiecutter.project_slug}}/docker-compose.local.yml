volumes:
  {{ cookiecutter.project_slug }}_local_postgres_data: {}
  {{ cookiecutter.project_slug }}_local_postgres_data_backups: {}
  {{ cookiecutter.project_slug }}_local_tailwind_cli: {}
  {% if cookiecutter.use_celery == 'y' %}{{ cookiecutter.project_slug }}_local_redis_data: {}{% endif %}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: {{ cookiecutter.project_slug }}_local_django
    container_name: {{ cookiecutter.project_slug }}_local_django
    depends_on:
      - postgres
      {%- if cookiecutter.use_celery == 'y' %}
      - redis
      {%- endif %}
      {%- if cookiecutter.observability == 'opentelemetry' %}
      - otel-collector
      {%- endif %}
      {%- if cookiecutter.mail_catcher == 'Mailpit' %}
      - mailpit
      {%- endif %}
      {%- if cookiecutter.mail_catcher == 'Mailtrap Local' %}
      - mailtrap-local
      {%- endif %}
    volumes:
      - /app/.venv
      # The Tailwind CLI, downloaded on first use: a volume, so the Linux binary is
      # fetched once, shared by the services and stays off the host
      - {{ cookiecutter.project_slug }}_local_tailwind_cli:/app/.django_tailwind_cli
      - .:/app:z
    env_file:
      - path: ./.envs/.local/.django
        required: false
      - path: ./.envs/.local/.postgres
        required: false
    ports:
      - '8000:8000'
    command: /start

  # Rebuilds <project_slug>/static/css/tailwind.css whenever a template or module changes
  tailwind:
    <<: *django
    image: {{ cookiecutter.project_slug }}_local_tailwind
    container_name: {{ cookiecutter.project_slug }}_local_tailwind
    depends_on:
      - postgres
    ports: []
    # Tailwind's CLI stops watching once its standard input closes
    tty: true
    command: python manage.py tailwind watch

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: {{ cookiecutter.project_slug }}_production_postgres
    container_name: {{ cookiecutter.project_slug }}_local_postgres
    volumes:
      {%- if cookiecutter.postgresql_version|int >= 18 %}
      - {{ cookiecutter.project_slug }}_local_postgres_data:/var/lib/postgresql/{{ cookiecutter.postgresql_version }}/docker
      {%- else %}
      - {{ cookiecutter.project_slug }}_local_postgres_data:/var/lib/postgresql/data
      {%- endif %}
      - {{ cookiecutter.project_slug }}_local_postgres_data_backups:/backups
    env_file:
      - path: ./.envs/.local/.postgres
        required: false

  {%- if cookiecutter.mail_catcher == 'Mailpit' %}

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: {{ cookiecutter.project_slug }}_local_mailpit
    ports:
      - "8025:8025"

  {%- endif %}
  {%- if cookiecutter.mail_catcher == 'Mailtrap Local' %}

  mailtrap-local:
    image: docker.io/mailtrap/mailtrap-local:latest
    container_name: {{ cookiecutter.project_slug }}_local_mailtrap
    ports:
      - "3550:3550"
    tmpfs:
      - /var/lib/mailtrap-local

  {%- endif %}
  {%- if cookiecutter.use_celery == 'y' %}

  redis:
    image: docker.io/redis:7.2
    container_name: {{ cookiecutter.project_slug }}_local_redis
    volumes:
      - {{ cookiecutter.project_slug }}_local_redis_data:/data

  celeryworker:
    <<: *django
    image: {{ cookiecutter.project_slug }}_local_celeryworker
    container_name: {{ cookiecutter.project_slug }}_local_celeryworker
    depends_on:
      - redis
      - postgres
      {%- if cookiecutter.mail_catcher == 'Mailpit' %}
      - mailpit
      {%- endif %}
      {%- if cookiecutter.mail_catcher == 'Mailtrap Local' %}
      - mailtrap-local
      {%- endif %}
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: {{ cookiecutter.project_slug }}_local_celerybeat
    container_name: {{ cookiecutter.project_slug }}_local_celerybeat
    depends_on:
      - redis
      - postgres
      {%- if cookiecutter.mail_catcher == 'Mailpit' %}
      - mailpit
      {%- endif %}
      {%- if cookiecutter.mail_catcher == 'Mailtrap Local' %}
      - mailtrap-local
      {%- endif %}
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: {{ cookiecutter.project_slug }}_local_flower
    container_name: {{ cookiecutter.project_slug }}_local_flower
    ports:
      - '5555:5555'
    command: /start-flower

  {%- endif %}
  {%- if cookiecutter.observability == 'prometheus' %}

  # Reads what the application exposes at /metrics, so the metrics can be seen while
  # developing. A deployment brings its own (docs/observability.rst). Its query API
  # asks for no credential, so it is published to this machine and no further.
  prometheus:
    image: docker.io/prom/prometheus:v3.14.0
    container_name: {{ cookiecutter.project_slug }}_local_prometheus
    depends_on:
      - django
    volumes:
      - ./compose/local/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:z
    ports:
      - '127.0.0.1:9090:9090'

  {%- endif %}
  {%- if cookiecutter.observability == 'opentelemetry' %}

  # Receives what the application exports and prints it, so the traces and metrics
  # can be read while developing: ``docker compose -f docker-compose.local.yml logs
  # otel-collector``. A deployment brings its own (docs/observability.rst). Nothing
  # is published to this machine, because the application reaches it by service name
  # and its output is the log.
  otel-collector:
    image: docker.io/otel/opentelemetry-collector:0.160.0
    container_name: {{ cookiecutter.project_slug }}_local_otel_collector
    volumes:
      - ./compose/local/otel-collector/config.yml:/etc/otelcol/config.yaml:z

  {%- endif %}
