volumes:
  test_postgres_data: {}
  test_postgres_data_backups: {}
  test_traefik: {}
  {%- if cookiecutter.cloud_provider == 'None' %}
  test_django_media: {}
  {%- endif %}
  {% if cookiecutter.use_celery == 'y' %}
  test_redis_data: {}
  {% endif %}


services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/production/django/Dockerfile

    image: {{ cookiecutter.project_slug }}_test_django
    {%- if cookiecutter.cloud_provider == 'None' %}
    volumes:
      - test_django_media:/app/{{ cookiecutter.project_slug }}/media
    {%- endif %}
    depends_on:
      - postgres
      - redis
    env_file:
      - ./.envs/.test/.django
      - ./.envs/.test/.postgres
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: {{ cookiecutter.project_slug }}_test_postgres
    volumes:
      {%- if cookiecutter.postgresql_version|int >= 18 %}
      - test_postgres_data:/var/lib/postgresql/{{ cookiecutter.postgresql_version }}/docker
      {%- else %}
      - test_postgres_data:/var/lib/postgresql/data
      {%- endif %}
      - test_postgres_data_backups:/backups
    env_file:
      - ./.envs/.test/.postgres

  traefik:
    build:
      context: .
      dockerfile: ./compose/production/traefik/Dockerfile
      args:
        ENVIRONMENT: test
    image: {{ cookiecutter.project_slug }}_test_traefik
    depends_on:
      - django
    volumes:
      - test_traefik:/etc/traefik/acme
    ports:
      - '0.0.0.0:80:80'
      - '0.0.0.0:443:443'
      {%- if cookiecutter.use_celery == 'y' %}
      - '0.0.0.0:5555:5555'
      {%- endif %}

  redis:
    image: docker.io/redis:7.2
    {% if cookiecutter.use_celery == 'y' %}
    volumes:
      - test_redis_data:/data
    {% endif %}

  taskworker:
    <<: *django
    image: {{ cookiecutter.project_slug }}_test_taskworker
    command: /start-taskworker


  {%- if cookiecutter.use_celery == 'y' %}

  celeryworker:
    <<: *django
    image: {{ cookiecutter.project_slug }}_test_celeryworker
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: {{ cookiecutter.project_slug }}_test_celerybeat
    command: /start-celerybeat

  flower:
    <<: *django
    image: {{ cookiecutter.project_slug }}_test_flower
    command: /start-flower
  {%- endif %}
  {%- if cookiecutter.cloud_provider == 'AWS' %}

  awscli:
    build:
      context: .
      dockerfile: ./compose/production/aws/Dockerfile
    env_file:
      - ./.envs/.test/.django
    volumes:
      - test_postgres_data_backups:/backups:z
  {%- endif %}
  {%- if cookiecutter.cloud_provider == 'None' %}

  nginx:
    build:
      context: .
      dockerfile: ./compose/production/nginx/Dockerfile
    image: {{ cookiecutter.project_slug }}_test_nginx
    depends_on:
      - django
    volumes:
      - test_django_media:/usr/share/nginx/media:ro
  {%- endif %}
